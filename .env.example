# Copy to `.env.local` and fill with real secrets (never commit `.env.local`).

# Pexels — optional; for future automated hero/inline images via Pexels API.
PEXELS_API_KEY=

# Firecrawl — optional; for research workflows (map/scrape external sites).
# https://www.firecrawl.dev
FIRECRAWL_API_KEY=
